# Production build defaults (baked in by `next build` on Vercel).
# Local `npm run dev` / local builds are overridden by `.env.local`.
NEXT_PUBLIC_FRAPPE_URL=https://crm.faip.pro
NEXT_PUBLIC_SITE_URL=https://faip.pro
