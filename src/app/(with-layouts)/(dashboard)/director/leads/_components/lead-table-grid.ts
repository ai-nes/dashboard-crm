// Shared row-grid column template for the Lead list (both the main Leads
// page and the campaign-detail lead list), so both stay pixel-aligned.
export const leadTableGrid =
  "lg:grid-cols-[minmax(0,1.1fr)_minmax(0,1.3fr)_minmax(0,0.95fr)_minmax(0,1.2fr)_minmax(96px,0.7fr)_minmax(120px,1.1fr)_minmax(0,1.25fr)_minmax(72px,0.8fr)]";
