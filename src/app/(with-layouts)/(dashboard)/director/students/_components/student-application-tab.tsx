import { ArrowRight, CheckCircle1, ClockThree } from "@tailgrids/icons";

import { Badge } from "@/components/tailgrids/core/badge";
import { Card, CardHeader, CardTitle } from "@/components/tailgrids/core/card";

import type { Student360SectionProps } from "./types";

const steps = [
  { label: "Tạo hồ sơ", detail: "Chưa bắt đầu", complete: false },
  { label: "Bổ sung giấy tờ", detail: "0/5 tài liệu", complete: false },
  { label: "Xét học bổng", detail: "Đề xuất 30%", complete: false },
  { label: "Xác nhận nhập học", detail: "Chưa thanh toán", complete: false },
];

export default function StudentApplicationTab({ data }: Student360SectionProps) {
  return <div className="grid items-stretch gap-5 xl:grid-cols-[minmax(0,1.2fr)_minmax(300px,0.8fr)]"><Card className="h-full p-5"><CardHeader className="mb-5"><div><CardTitle>Tiến độ hồ sơ ứng tuyển</CardTitle><p className="mt-1 text-xs leading-5 text-text-tertiary">Theo dõi các bước chuyển đổi từ quan tâm sang nhập học.</p></div><Badge color="warning">Chưa bắt đầu</Badge></CardHeader><div className="rounded-lg bg-badge-primary-background p-4"><p className="text-xs text-badge-primary-text">Nguyện vọng ưu tiên</p><div className="mt-1 flex flex-wrap items-center justify-between gap-2"><p className="text-lg font-semibold text-text-primary">{data.application[0]?.value}</p><Badge color="primary">Đợt 2 · 2026</Badge></div><p className="mt-2 text-xs text-text-secondary">Hạn hoàn tất dự kiến: <strong className="text-text-primary">còn 12 ngày</strong></p></div><ol className="mt-5 space-y-4">{steps.map((step, index) => <li key={step.label} className="relative flex gap-3"><span className={`z-10 flex size-7 shrink-0 items-center justify-center rounded-full border-2 ${step.complete ? "border-success-500 bg-badge-success-background text-success-500" : "border-card-border bg-card-background text-text-tertiary"}`}>{step.complete ? <CheckCircle1 size={15} /> : <span className="text-xs font-semibold">{index + 1}</span>}</span>{index < steps.length - 1 && <span className="absolute top-7 bottom-[-16px] left-3.5 w-px bg-card-border" aria-hidden="true" />}<div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><p className="text-sm font-medium text-text-primary">{step.label}</p><span className="text-xs text-text-tertiary">{step.detail}</span></div><div className="mt-2 h-1.5 rounded-full bg-background-soft-200"><div className="h-full w-0 rounded-full bg-primary-500" /></div></div></li>)}</ol></Card><Card className="h-full p-5"><CardHeader className="mb-5"><div><CardTitle>Điều kiện chuyển đổi</CardTitle><p className="mt-1 text-xs leading-5 text-text-tertiary">Các yếu tố đang tác động đến quyết định.</p></div></CardHeader><dl className="divide-y divide-card-border">{data.application.slice(1).map((item) => <div key={item.label} className="flex items-center justify-between gap-4 py-3 first:pt-0 last:pb-0"><dt className="text-sm text-text-secondary">{item.label}</dt><dd>{item.status ? <Badge color={item.status === "warning" ? "warning" : item.status === "success" ? "success" : "primary"}>{item.value}</Badge> : <span className="text-sm font-medium text-text-primary">{item.value}</span>}</dd></div>)}</dl><div className="mt-5 rounded-lg border border-warning-200 bg-badge-warning-background p-4"><div className="flex items-start gap-2"><ClockThree size={16} className="mt-0.5 shrink-0 text-warning-500" /><div><p className="text-sm font-semibold text-badge-warning-text">Rủi ro bỏ lỡ thời hạn</p><p className="mt-1 text-xs leading-5 text-text-secondary">Chưa có tài liệu nào được tải lên. Nên xử lý sau cuộc gọi với phụ huynh.</p></div></div></div><button type="button" className="mt-5 inline-flex items-center gap-1 text-sm font-medium text-primary-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-500">Xem checklist hồ sơ <ArrowRight size={15} /></button></Card></div>;
}
